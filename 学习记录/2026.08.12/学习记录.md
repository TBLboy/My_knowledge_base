# Day 5：nn.Module 与 optimizer

- 学习路线：机器人学习系统路线
- 日期：2026-08-12
- 设备：Linux RTX 4090；脚本使用 CPU，因此 Windows RTX 4060 也应可直接运行
- 今日任务：使用 `nn.Linear` 和 `torch.optim.SGD` 重写线性回归，保存并加载 `state_dict`
- 实际用时：待填写

## 闭卷回忆

今天开始前，我能回答：

- 待填写

## 核心理解

用自己的话说明今天最重要的 1-3 个概念：

- 待填写

AI 复核备注（不替代本人理解）：

- `nn.Module` 把参数、前向计算和工具方法组织在一个对象中。
- `optimizer.step()` 负责按梯度更新参数，`optimizer.zero_grad()` 负责清空旧梯度。
- `state_dict` 只保存参数和持久缓冲区，不包含数据、优化器状态或模型结构。

## 代码与实验

- 入口：`exercises/day05_module_optimizer.py`
- 配置：`nn.Linear(1, 1)`、`nn.MSELoss`、`optim.SGD(lr=0.05)`、500 epoch
- 结果：
  - `model=Linear(in_features=1, out_features=1, bias=True)`
  - `parameter weight: shape=(1, 1), requires_grad=True`
  - `parameter bias: shape=(1,), requires_grad=True`
  - initial loss：`18.185162`
  - final loss：`0.055471`
  - learned weight：`2.9880`
  - learned bias：`1.9562`
  - `loaded_prediction_matches=True`
- 设备与显存：本次运行使用 CPU；小模型不依赖显存
- checkpoint：`results/day05_state_dict.pt`

## 错误记录

| 症状 | 根因 | 修复 | 如何证明已修复 |
| --- | --- | --- | --- |
| 待填写 | | | |

## 今日结论

Day 5 作业代码已由陪练代理生成并验证，state_dict 保存和加载后的预测完全一致。闭卷回忆、核心理解和错误记录等待本人完成。

## 未解决问题

- 待填写

## 下一步

完成 Day 5 个人理解与错误记录后进入 Day 6：本周复盘实验。
