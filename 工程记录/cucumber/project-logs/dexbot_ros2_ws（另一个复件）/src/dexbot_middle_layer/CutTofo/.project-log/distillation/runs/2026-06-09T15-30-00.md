# Distillation Run — 2026-06-09T15:30:00

## Scan scope

- Project: CutTofo (cuttofo)
- Source files read:
  - `progress.md` (all entries, 2026-06-06 through 2026-06-08)
  - `current-session.md`
  - `business-logic/decision-records.md`
  - `debugging/known-issues.md`
  - `distill-input/candidate-insights.md`
  - `distill-input/debugging-lessons.md`
  - `distill-input/reusable-patterns.md`
  - `distillation/state.yaml`
  - `distillation/candidate-register.md`
- Previous distillation: 2026-06-09T13:25:00 (captured cuttofo-0001, 0002, 0003)
- Existing KB entries checked: 6 total (3 from previous distillation + 3 debugging from KB optimization)

## Changes since last scan

The previous distillation captured 3 architecture/workflow/config patterns from the early progress.md entries and distill-input/ summaries. Since then, progress.md has accumulated 15+ additional engineering episodes spanning:

- Workspace environment hygiene and underlay pollution (two separate deep-clean rounds)
- GUI/rclpy runtime environment bootstrapping (3-layer environment problem)
- RealSense camera topic namespace debugging
- SAM3 vision prompt collision between parallel workflows
- Return path recovery (cartesian return after cut)
- Vertical cut phase7 migration
- Phase6 perception parameter override architecture
- Prepare target geometry ownership cleanup
- Visualizer package creation and vendoring
- Handle-approach + tofu workflow integration

## Candidates identified

### cuttofo-0004 — ROS2 workspace environment isolation is not automatic

- **Category**: workflow / debugging
- **Confidence**: high
- **Reusability**: high
- **Suggested action**: add
- **Source refs**:
  - progress.md: 2026-06-07 14:35 CST (first hygiene cleanup, entries 328-375)
  - progress.md: 2026-06-07 14:35 CST (second deep clean, entries 460-519)
- **Evidence summary**: After duplicating a ROS2 workspace (tofu → cucumber), the old workspace's paths were silently baked into `install/setup.bash` because the build shell had sourced the old underlay. Environment variables (`AMENT_PREFIX_PATH`, `COLCON_PREFIX_PATH`, `CMAKE_PREFIX_PATH`, `PYTHONPATH`, `LD_LIBRARY_PATH`) all carried the old workspace as a permanent dependency. Fixing `~/.bashrc` alone was insufficient — the install artifacts themselves were contaminated. Resolution required rebuilding the entire workspace in `env -i` with only `/opt/ros/humble/setup.bash`.
- **Reusable rule**: When a ROS2 workspace is cloned, moved, or duplicated, always rebuild in a clean shell (`env -i bash --rcfile <(echo 'source /opt/ros/humble/setup.bash')`). Never trust that `install/setup.bash` from a prior build is self-contained — it snapshots the underlay chain of its build environment.
- **Applicability**: Any ROS2 project that has been moved, copied, or shared across machines.
- **Related existing entries**: `config-behavior/trace-runtime-consumers-before-tuning.md` (shared theme: environment state is not what config files alone declare)

### cuttofo-0005 — Python tools embedding rclpy need full ROS2 runtime, not just sys.path

- **Category**: debugging
- **Confidence**: high
- **Reusability**: high
- **Suggested action**: add
- **Source refs**: progress.md: 2026-06-07 19:56 CST (GUI startup fix, entries 169-202)
- **Evidence summary**: A Tkinter GUI directly launching with `python3 main.py` failed progressively through 3 layers: (1) missing source package in `sys.path`, (2) missing ROS2 message Python packages, (3) `rclpy.create_client()` failing with "Could not load library libdexbot_interfaces_low__rosidl_typesupport_fastrtps_c.so". Layers 1-2 could be fixed with `sys.path` patching, but layer 3 required the full ROS2 runtime environment (`LD_LIBRARY_PATH`, `AMENT_PREFIX_PATH`). The final fix auto-sources `install/setup.bash` and `exec()`s the same script with the enriched environment, rather than trying to manually reconstruct it.
- **Reusable rule**: Python scripts that call `rclpy.create_node()`, `create_client()`, or `create_subscription()` need the full ROS2 runtime environment — not just Python import paths. The typesupport shared libraries are loaded dynamically and depend on `AMENT_PREFIX_PATH` + `LD_LIBRARY_PATH`. The robust pattern is: detect missing environment → `source install/setup.bash` → `exec` yourself.
- **Applicability**: Any Python GUI, CLI tool, or standalone script that embeds ROS2 client libraries.
- **Related existing entries**: `debugging/ros2-subscriber-no-message-qos.md` (shared theme: ROS2 middleware failures are often silent and environmental)

### cuttofo-0006 — Tick-driven state machine is sufficient for predominantly linear robot workflows

- **Category**: architecture
- **Confidence**: medium
- **Reusability**: medium
- **Suggested action**: add
- **Source refs**: `business-logic/decision-records.md` (first decision record)
- **Evidence summary**: The CutTofo orchestrator uses a simple tick-driven state machine (20Hz loop, explicit state enum, one active state at a time) to coordinate 7+ action skills. Despite the workflow having conditional branches (handle_approach optional, error handling), the tick-driven approach proved adequate and easier to debug than Behavior Trees or SMACH. The workflow is predominantly linear: approach → prepare → cut_round → prepare → cut_round → prepare → vertical_cut.
- **Reusable rule**: For robot workflows that are mostly sequential with occasional optional steps, a simple tick-driven state machine (state enum + loop at fixed frequency) is often sufficient. Reserve Behavior Trees for workflows with significant branching, parallel execution, or runtime re-planning. The tick-driven approach wins on debuggability: a single `print(f"[{self._state}]")` gives complete visibility.
- **Applicability**: Linear or mostly-linear multi-step robot task orchestration.
- **Related existing entries**: `workflow/single-purpose-launches-and-central-orchestration.md` (shared theme: keep orchestration explicit and central)

### cuttofo-0007 — Multi-axis tasks benefit from axis-specific control modes rather than one global mode

- **Category**: patterns
- **Confidence**: medium
- **Reusability**: medium
- **Suggested action**: add
- **Source refs**: `business-logic/decision-records.md` (third decision record)
- **Evidence summary**: The tofu cutting task uses impedance control for horizontal cut_round (force compliance to avoid crushing tofu) and position control for vertical_cut (precision to ensure complete cut-through). Using the same control mode for both would either crush the tofu (all position) or lose vertical precision (all impedance).
- **Reusable rule**: When a manipulation task spans multiple physical directions or phases with different requirements (compliance in one axis, precision in another), use different control modes per phase rather than one global mode. The control strategy should match the physical constraint, not the tool identity.
- **Applicability**: Multi-phase manipulation tasks where physical requirements differ between phases.
- **Related existing entries**: None direct.

## Comparison with existing KB

| Candidate | Existing KB overlap | Verdict |
|---|---|---|
| cuttofo-0004 | `trace-runtime-consumers-before-tuning.md` (loose thematic overlap) | New — distinct topic (workspace build environment vs parameter tracing) |
| cuttofo-0005 | `ros2-subscriber-no-message-qos.md` (shared ROS2 silent-failure theme) | New — distinct topic (runtime linking vs middleware QoS) |
| cuttofo-0006 | `single-purpose-launches-and-central-orchestration.md` (shared orchestration theme) | New — distinct topic (control flow pattern vs launch composition) |
| cuttofo-0007 | None | New |

## Notes

- The 3 debugging entries in KB (kb-debugging-0001/0002/0003) were created through direct KB optimization, not through the distillation pipeline. They are complementary to these project-level candidates.
- `distill-input/anti-patterns.md` and `open-knowledge-questions.md` are still template-only — no project-specific content yet.
- `debugging/known-issues.md` contains only placeholder-level records ("切黄瓜待优化"), not reusable lessons.
- The progress.md entries contain additional debugging episodes (RealSense topic namespace, SAM3 prompt collision, cartesian return recovery) that could become candidates in a future scan if they recur across projects.

## Draft results

- **Staging location**: `/home/tbl/personal-knowledge-base/staging/cuttofo/2026-06-09T15-30-00/add/proposals.md`
- **All 4 candidates proposed as `add`** — no conflicts with existing 6 KB entries.
- Ready for `apply` after review.

## Apply results (2026-06-09 ~16:00)

All 4 candidates accepted and written to formal KB entries:
- `workflow/ros2-workspace-environment-isolation.md` (kb-workflow-0002)
- `debugging/rclpy-tools-need-full-ros2-runtime.md` (kb-debugging-0004)
- `architecture/tick-driven-state-machine-linear-workflows.md` (kb-architecture-0002)
- `patterns/axis-specific-control-modes.md` (kb-patterns-0001)

Index rebuilt: 10 entries total. KB distillation ledger updated.

## KB scaling strategy notes (2026-06-09)

Discussed concern: as projects and KB entries accumulate, will the KB become unwieldy?

Current mitigation already in place:
- `kb_hotload.py` injects top-3 only
- `kb.py` scoring filters low-relevance noise
- `session-rules.yaml` trigger keywords provide first-pass coarse filtering
- Category directories (debugging/architecture/patterns/...) provide physical isolation

Future strategies to implement when scale demands (~50+ entries):
1. **Merge over stack** — new evidence on same topic should `merge`/`update` existing entries, not create new ones (candidate-register already supports `merged`/`superseded` status)
2. **Confidence decay** — entries not hit/refed for long periods auto-lower confidence, reducing query weight
3. **Scope tagging** — mark entries as "universal" vs "domain-specific"; non-matching domains skip injection
4. **Two-tier retrieval** — coarse filter by trigger/category first, then scoring fine-rank, avoiding full O(n) scan

These are noted but NOT implemented now — currently 6 formal entries + 4 candidates. Revisit at ~50 entries.
